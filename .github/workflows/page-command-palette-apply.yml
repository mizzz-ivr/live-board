name: Page Command Palette Apply

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.head_ref == 'feat/page-command-palette'
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/page-command-palette
          fetch-depth: 0

      - name: Apply page rename domain command
        run: python .github/scripts/apply-page-rename-domain.py

      - name: Apply page command palette
        run: python .github/scripts/apply-page-command-palette.py

      - name: Remove temporary implementation files
        run: |
          rm .github/scripts/apply-page-rename-domain.py
          rm .github/scripts/apply-page-command-palette.py
          rm .github/workflows/page-command-palette-apply.yml

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.15.1
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E smoke tests
        run: pnpm test:e2e

      - name: Commit verified implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Page操作をコマンドパレットへ追加"
          git push origin HEAD:feat/page-command-palette
