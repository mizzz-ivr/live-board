name: Apply workspace home README

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/apply-workspace-home-readme.yml'
      - 'scripts/apply-workspace-home-readme.mjs'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/workspace-home
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Update README
        run: node scripts/apply-workspace-home-readme.mjs
      - name: Remove temporary files
        run: |
          rm .github/workflows/apply-workspace-home-readme.yml
          rm scripts/apply-workspace-home-readme.mjs
      - name: Commit changes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "READMEへワークスペースホームの利用方法を追記"
          git push origin HEAD:feat/workspace-home
