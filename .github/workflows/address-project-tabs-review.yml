name: Address Project Tabs Review

on:
  push:
    branches:
      - feat/project-tabs
    paths:
      - .github/workflows/address-project-tabs-review.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feat/project-tabs
          fetch-depth: 0

      - name: Apply review fixes
        run: python .github/scripts/address-project-tabs-review.py

      - name: Commit review fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Projectタブのレビュー指摘へ対応"
          git push origin HEAD:feat/project-tabs
